/**
 * Blocking Module Index
 * Exports blocking key generation utilities
 */

export {
  generateBlockingKeys,
  generateColognePhonetic,
  createBlocks,
  getCandidatePairs,
  STANDARD_BLOCKING_CONFIGS,
  type BlockingKeyConfig,
  type BlockingMethod,
  type BlockingOptions,
  type BlockingKey,
} from './blockingKeys.js';
